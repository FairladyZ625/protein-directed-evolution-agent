# Facts

Managed by `ha fact record`; hand edits are rejected.

## Records

### F-43D8C747

- Statement: 四个并行修复包各自正确但两两语义不兼容,集成后 5 个点名测试失败;其中一条是真设计缺陷。隔离验证:w-campaign 单独跑 tests/test_campaign.py 10 passed、w-pipeline 单独跑 tests/test_agent.py 10 passed,四分支文件面互不重叠(campaign.py+test_campaign.py / pipeline.py+test_agent.py / knowledge+test_knowledge.py / events+test_events.py)且合并零冲突,故失败是语义耦合不是文件冲突。配对复现:w-campaign+w-pipeline → 3 failed(test_campaign);w-pipeline+w-knowledge → 2 failed(test_agent)。两条根因:(1)包2 给 run_pipeline 加了『候选必须与 149361 实测集合求交』这一正确约束,而包1 的合成夹具 candidate_space_size=16 且未提供实测集合,候选被全过滤,n_nominated 从期望 [2,2] 变成 [];修复方向是补夹具的实测池,不是削断言。(2)**包3 把 R-PRIORITIZE-HISTORICAL 实现成了硬门而非排序建议**:无历史数据时它返回 pass=False(note='historically high-fitness single mutants: none'),而 agent/pipeline.py 的 ScientificCritic 判定是 ok=all(x['pass'] for x in rules),于是全部候选被拒、PipelineResult.accepted 为空。『优先历史好单点』在语义上是 prioritization,不是 viability gate,不得参与拒收判定;这是实现方向错误,须改代码。附带观察:包2 已在 AnalystReport 加入 position_gains(如 {39:2.0,40:1.8,41:0.0,54:1.5}),即笔试题『结果展示 d-i 发现哪些位点可能重要』已部分交付,task_df963f97f7ae5411853967621d 的范围可相应缩小。
- Evidence source: .worktrees/stage 上 w-campaign/w-pipeline/w-knowledge/w-events 四分支的零冲突合并与配对隔离测试; tests/test_campaign.py:47; tests/test_agent.py:197; agent/pipeline.py 的 ScientificCritic 判定式
- Observed at: 2026-09-12T13:52:31.264Z
- Confidence: high
- State: standing

