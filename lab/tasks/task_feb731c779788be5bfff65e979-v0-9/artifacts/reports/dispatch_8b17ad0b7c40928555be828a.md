verdict：打回修改（`changes_requested`），已记录独立评审。

证据：

- 契约测试：16 passed；全套测试：167 passed。
- 四臂事件流可读取；v08 不分叉，v09 分叉；v09 反思臂 `excluded_motifs` 为 5/6。
- `tests/test_auto_researcher.py` 实际不存在。
- 任务声明的 `tmp/v09-contract/` 未提交，产物位于 `lab/reports/v09-contract/`。
- 引用检查仍报告全仓 5 处失效引用。

修复方向：补齐或正式登记指定测试路径；补齐数据落点，或经治理修改任务契约并提供明确映射；closeout 中严格区分任务范围内与全仓引用检查结果。