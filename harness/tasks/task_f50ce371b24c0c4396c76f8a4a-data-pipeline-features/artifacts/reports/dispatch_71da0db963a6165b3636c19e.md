# Independent execution review

## Verdict

changes_requested（打回）

## Tool-grounded evidence

- 本 dispatch 冻结 submission digest 为 `sha256:ca6ca0150320117a719d5f24fca23f941f6e70602ea52fcec304be1a51d0e868`，冻结 delivery commit 为 `91d9a17154617c0b068a44bcb99f1e44c1ee7fd1`；execution 投影仍为 iteration 2、submitted、该 commit，未观察到换 cut。
- `git show --stat/--name-status 91d9a...` 显示该提交自身只修改 `HARNESS-UX-FEEDBACK.md`；冻结 Deliverables 也只列出该文件，未包含契约要求的 `data/`、`evolution/mutations.py`、`features/` 或 `tests/test_data_pipeline.py`。
- 为避免用工作区漂移代替冻结内容，使用 `git archive 91d9a...` 构造临时只读快照，仅补入契约声明为 gitignored 的原始 GB1 CSV；在该快照运行 `.venv/bin/pytest -q tests/test_data_pipeline.py`，真实输出为 `6 passed in 0.95s`。
- 冻结树内三池实测为 5,000 / 50,000 / 94,361，交集均为 0，并集 149,361，WT `VDGV` 位于 train。
- 冻结 one-hot 缓存为 variants `(149361,)`、embeddings `(149361, 80)`、`uint8`；冻结有序 train ESM 缓存 `...-dbfd1ecc45bd4b72148d.npz` 为 variants `(5000,)`、embeddings `(5000, 1280)`、`float16`，顺序与 train pool 一致、全为有限值且非恒定（标准差约 `0.24883`）。
- 缓存检测器阳性对照：完整缓存连续读取时注入 backend 调用数保持 1；人为交换缓存 variants 顺序后再次读取，调用数从 `1 -> 2`，证明失配检测路径确实会发声。
- 定向 ESM 单测只使用 deterministic injected backend；本轮没有运行真实 ESM-2 650M forward，因此不把该测试表述为真实模型与缓存逐元素一致性证据。

## Defects

1. **Blocking — 冻结 delivery 不承载任务交付。** Task contract 的交付物是代码、三池与特征缓存，执行面限定为 `data/`、`evolution/mutations.py`、`features/` 和本任务测试；本 execution 的冻结 Deliverables 只有无关的 `HARNESS-UX-FEEDBACK.md`。冻结 commit 的树中虽因历史快照含有可工作的 T2 文件，但“树中存在”不能证明本 execution 交付了它们。
2. **Blocking — completion claim 与机器推导的 deliverables 不一致。** completion claim 声称交付统一数据/特征底座，而唯一机器登记的 deliverable 是 Harness UX 文档。按契约与 content-pinned review 规则，不能用本地文件、祖先内容或未登记路径补足冻结交付。
3. **Evidence limitation — 真实 ESM-2 提取一致性仍未独立验证。** 已提交缓存可读取、形状和内部一致性成立，且 closeout 对本地权重依赖披露诚实；但真实 650M forward 与缓存对照仍是未验证项。

## Actionable repair

- 再次 amend/resubmit 时，让机器冻结的 deliverables 明确包含任务自有的三池、数据校验与突变代码、one-hot/ESM 编码器、完整 one-hot 缓存、有序 5,000-row train ESM 缓存及 `tests/test_data_pipeline.py`；不得以无关文档提交作为 delivery cut。
- 保持交付 cut 仅覆盖 task contract 的 execution surface，并在提交前核对 `ha task submit --amend` 推导出的 Deliverables，而不是只核对 commit 树里是否碰巧已有文件。
- 继续保留当前 ESM 权重/clean-env 风险说明；除非附真实 650M forward 对照，否则将相关测试准确描述为“注入 backend 的缓存语义测试”。
