# 数据缓存不入库
data/*.zip
# GB1 真值表现已入库(44 MB,GitHub 单文件上限内)。评审 clone 后模块③的自动推荐
# 需要它做真值校验;不入库的话页面只能给模型预测,拿不到实测对照。
# AAV 的 full_data.csv 是 267 MB,超过 GitHub 100 MB 硬上限,仍不入库——
# 但看板不需要它(AAV 面板读的是已入库的预计算 JSON),只有重跑 AAV 实验才需要。
data/splits/
data/*.fasta
# SQLite 运行时产物
*.db
*-wal
*-shm
# 本地密钥（池子 token 等），只提交 .env.example
.env
# Python
__pycache__/
*.pyc
.venv/
.DS_Store
# 台账投影与本地状态由 daemon 物化,不入库;交付文档树在 lab/
harness/
/.harness/
lab/reports/cache/
.worktrees/

# 更难数据集原始文件(不入库,同 GB1)
data/aav/*.csv
data/aav/*.zip
data/avgfp/*.csv
data/avgfp/*.zip

# ESM 嵌入缓存(大,不入库)
features/cache/

# 临时工作区与系统垃圾(2026-09-12 交付前整理)
/tmp/
.DS_Store
