# Facts

Managed by `ha fact record`; hand edits are rejected.

## Records

### F-363846B5

- Statement: 五角色流水线里的 Critic 角色,LLM 接线从来没成功过一次:evolution/campaign.py 的 _llm_critic 返回裸字符串,而 agent/pipeline.py 的 _structured_call 会把端口返回值送进 CriticReview.model_validate(),后者要求带 note 字段的映射,于是每次调用都抛 ValidationError 被 except 吞掉、静默降级成确定性注释。症状:一次 --use-llm 真跑的 critique 列表 15/15 全是「accepted (deterministic critic fallback)」,而同一端点手工探测 19 秒正常应答(gpt-5.6-sol via pool)。此前任何「LLM 评审」的表述都不成立。知识消融报告记录的「36/36 agent 轮次超时」很可能是同一机制的另一表现。修复 52b2dd6:返回 {note:...}、空回复算失败、新增 llm_budget 上限(修好 measurable_variants 后一轮上千候选 x 19 秒会跑几小时)。机制教训:把提供方端口包在 schema 边界后面,边界两侧的返回形状必须有测试钉住——否则 except Exception 会把契约违约伪装成「提供方不可用」,而 fallback 让系统看起来一切正常。
- Evidence source: evolution/campaign.py:171-196; agent/pipeline.py:71-83,170-205; commit 52b2dd6; tests/test_campaign.py 两条反向验证过的新测试
- Observed at: 2026-09-12T15:37:19.867Z
- Confidence: high
- State: standing

