## 背景

裁定 D2 原文锁定「ESM-2 650M 本地实时(L2 主力)+ one-hot(L1),嵌入缓存进仓库」。试题覆盖度审计(`harness/context/research/assignment-coverage-audit.md`)判定 D2 **打回**:GB1 闭环(`evolution/campaign.py:56-62`)与 Demo(`app/demo.py:104-112,532-536`)的实际 predictor 都是 80 维 one-hot,ESM-2 只出现在预测器阶梯评测里。

包A 奉命把 ESM-2 接回主力时撞到硬约束并正确停手(fact `F-54695D51`):已提交的 1280 维缓存只覆盖 5000、2168 与两个 2000 样本块,没有 149,361 全空间覆盖。补齐需要约 765MB(float32),不可提交。

## 裁定

采纳 CH1,修订 D2 而不是补数据。

新口径:**ESM-2 650M 定位为预测器阶梯上的表征对照**,用已提交的样本块缓存保证评委可复现;**闭环 campaign 使用 one-hot 特征**,并在产物中记录 feature provenance,让每一跑用了哪一层可被追溯。报告把「ESM 未带来稳定增益」作为诚实负结果正面写出来。

驳回 RJ1(花 765MB 去兑现一个实测不支持的声称,且试题明确不要求训练大模型)、RJ2(回避即是把已知的声称-实现不一致带进交付物)、RJ3(本地生成不提交在技术上可行,但前提仍未被实测支持,留作 future-work)。

判据是实测而非偏好:GB1 predictor ladder 上 ESM-2 只在 ridge(0.4925 vs 0.4840)与 mlp(0.4914 vs 0.3892)略胜,在 xgboost 上反而大幅落后(0.3768 vs 0.4741);v0.3 的 ESM zero-shot 更是实测失败。

## 影响

- 包A(`task_0bc2d4d678eea24c4930b60a0e`)第 3 项由「把 ESM-2 接回主力」改为「记录 feature provenance 并在报告中说明分层口径」。
- 最终报告 v2.0 的「适应度预测模型」章须按本裁定写:ESM 是对照不是主力,并附上未带来稳定增益的实测数字。
- README 的表征描述同步更新(包C 文件面)。
- 原 D2 不删除,以修订记录保留——声称曾经是什么、为什么改,本身是审计链的一部分。
