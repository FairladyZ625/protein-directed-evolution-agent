# Execution exe_be129351d60870d0d001591008

Managed by `ha task start/submit`; hand edits are rejected.

- Task: task_2e485a1ae862bdec5fcc7c9677
- Iteration: 0
- State: accepted
- Claimed: 2026-09-12T04:56:14.755Z
- Submitted: 2026-09-12T13:07:51.276Z
- Closed: 2026-09-14T13:26:03.961Z
- Commit: 471536b18ff33bfbe8e2082009aa2e9e450d7cf2
- Completion claim: Implemented agentic v0.6 meta-layer backtrack in `agent/auto_researcher.py`: campaign-trajectory stagnation tracking (`top10_max_history`, `rounds_since_improvement`, `STALL_THRESHOLD=2`), a `redirect_batch(n, max_overlap)` basin-hop tool (high predicted-mean candidates whose mutation composition differs from the measured top-10 cluster signature), a pure-exploitation default acquisition for backtrack campaigns (`v06_pure_exploit_default`, the v0.5 rank-45 lesson), and two autonomy variants via `--backtrack full|semi` (full = the LLM judges stagnation itself; semi = a hard rule injects `stalled=true` into the prompt; both keep C+A and byte-identical v0.5 prompts when backtrack is off). Ran both variants on AAV seed 42 / 288 budget / same gate / same EpistasisRidge surrogate / gpt-5.6-sol: both reach cum_top10_max 7.829, strong 166 (best across v0.1–v0.6), 0 tool errors, event chains verified. Result is an honest negative on the 8.416 goal with a nailed mechanism: both stall detections fired correctly at round 5, but the best-cluster signature was empty (top shared substitution T20E 4/10 < 5/10) so redirect degenerated to the same greedy top-48; post-hoc read-only counterfactuals show pure predicted-mean tops out at exactly 7.829 (replay matches the v0.6 batches batch-for-batch; peak never measured) and that the v0.4 8.4162 reference line's peak arrived in its round-4 diverse(mean+3sd) batch (peak UCB-rank #15, mean-rank #2930 at that state) — i.e. the "pure-greedy 8.416" premise is an exploit/UCB alternation, and a mean-only decision surface cannot reach 8.416 regardless of autonomy level.

交付提交:`471536b18ff33bfbe8e2082009aa2e9e450d7cf2`(v0.6 元层回溯自主)。
- Reviews: rev-de-reviewer-v06-20260912/approved
- Selected review: rev-de-reviewer-v06-20260912
- Consent: consent-2b149208a4ae372adab9736e
- Checker witnesses: ci/op_5bcd50866d2618fcc0e8f2a76d77e6238171964e9ca6842f45050e37d66edc8e
- Code-doc witness: code-doc-e596917c5ca6d6b2

## Deliverables

- agent/auto_researcher.py
- lab/reports/agentic-v0.5/aav/agentic.events.jsonl
- lab/reports/agentic-v0.5/aav/agentic.metrics.json
- lab/reports/agentic-v0.5/aav/figures/agentic.png
- lab/reports/agentic-v0.5/manifest.json
- lab/reports/agentic-v0.5/report.md
- lab/reports/agentic-v0.6/aav/figures/curves.png
- lab/reports/agentic-v0.6/aav/full/agentic.events.jsonl
- lab/reports/agentic-v0.6/aav/full/agentic.metrics.json
- lab/reports/agentic-v0.6/aav/full/figures/agentic.png
- lab/reports/agentic-v0.6/aav/semi/agentic.events.jsonl
- lab/reports/agentic-v0.6/aav/semi/agentic.metrics.json
- lab/reports/agentic-v0.6/aav/semi/figures/agentic.png
- lab/reports/agentic-v0.6/manifest.json
- lab/reports/agentic-v0.6/report.md
- models/train_ladder.py
- tests/test_auto_researcher_backtrack.py
- tests/test_auto_researcher_compose.py
- tests/test_auto_researcher_gate.py
- tests/test_epistasis_surrogate.py

## Outputs

- none

## Verification

- - Targeted tests: `PYTHONPATH=. .venv/bin/python -m pytest tests/test_auto_researcher_backtrack.py tests/test_auto_researcher_compose.py tests/test_auto_researcher_gate.py tests/test_epistasis_surrogate.py -q` → **28 passed** (13 new: rising-curve no-false-stall negative control, redirect fitness-blindness under shuffled pool labels, v06 default ratio 1.0 from round 1, v05 adaptive default unchanged, prompt paragraph swap).
- v0.5 SYSTEM_PROMPT byte-identical when backtrack is off (compared against `git show HEAD:agent/auto_researcher.py`).
- Campaigns (real runner output): full → `llm_used=True model=gpt-5.6-sol budget_spent=288 tool_calls=25 backtrack=full redirects=[5] stall_flags=[] cum_top10_max=7.829 strong=166`; semi → `... budget_spent=288 tool_calls=27 backtrack=semi redirects=[5] stall_flags=[5,6] cum_top10_max=7.829 strong=166`; both event chains pass `EventStore.verify()` (25/27 events), 0 `agent.tool.error`/`agent.llm.round_error`/`agent.llm.no_test`, `n_gate_rejected=0`.
- Answer-agnostic guards: stall rule reads only measured fitness trajectory; redirect ranks by surrogate predicted mean + measured-cluster signature only; unit test proves picks are invariant to shuffling pool true fitness; all counterfactuals are post-campaign read-only (never fed back).
- Fact promoted: `F-72B1D940` (evidence `lab/reports/agentic-v0.6/report.md`, confidence high, task-linked).

## Known gaps

- - Single dataset/seed/model run; full-vs-semi equivalence rests on one stall event; the 8.416 decomposition relies on the v0.4 artifact's tool_trace plus deterministic replay (rng noise prevents bit-exact reproduction of r2+ alternation batches — the "8.4162 came from a diverse batch" fact itself is directly recorded in tool_trace, not inferred).
- GLM-5.3 was not on the endpoint's model list; gpt-5.6-sol used instead (same as v0.4/v0.5) and recorded in report/manifest per the task_plan note.
- `lab/reports/experiment_log.jsonl` was appended by the runs (designed behavior) but left unstaged — outside the declared write surface; the spine commit is CEO-owned.
- Task branch has no merge-base with origin/main (same as v0.5), so the pre-handoff rebase cannot be performed safely.
- cold-start 9.536 incumbent is again called "best measured" in LLM final summaries (known metric-vs-summary caveat; the reported metric counts only the 288 tested variants).
- Mechanism sentence: **an escape action that re-ranks candidates by the same statistic (predicted mean) as the strategy it is escaping cannot leave that strategy's basin — it degenerates to the greedy batch whenever the escape filter (here: a shared-mutation signature) is empty.** Search: `git grep -n "signature\|redirect\|diverse" agent/ evolution/ models/` plus reading the acquisition paths (`agent/auto_researcher.py` `list_pool`/`compose_batch`/`redirect_batch`, `evolution/pool_campaign.py` `_rank`). Found: the sibling is `list_pool(by="diverse")` / the v0.4 deterministic alternation — the only acquisition in the repo that carries a variance term (mean+3√var), which is exactly why it, and not any mean-only path, reached 8.4162 (peak UCB-rank #15 vs mean-rank #2930 at the r3 state); no other caller ranks by composition or variance, so no other site silently suffers the degeneration, but any future reuse of `_best_cluster_signature` on a landscape whose measured top-10 lacks a ≥50%-shared substitution will reproduce it. Documents that inherit the falsified premise ("deterministic pure-greedy 8.416"): `agentic-v0.4/report.md`, `agentic-v0.5/report.md`, fact F-F7BC7554, `frontier-landscape-synthesis.md` — re-annotation is CEO-owned.

## Residual risks

- - Single dataset/seed/model run; full-vs-semi equivalence rests on one stall event; the 8.416 decomposition relies on the v0.4 artifact's tool_trace plus deterministic replay (rng noise prevents bit-exact reproduction of r2+ alternation batches — the "8.4162 came from a diverse batch" fact itself is directly recorded in tool_trace, not inferred).
- GLM-5.3 was not on the endpoint's model list; gpt-5.6-sol used instead (same as v0.4/v0.5) and recorded in report/manifest per the task_plan note.
- `lab/reports/experiment_log.jsonl` was appended by the runs (designed behavior) but left unstaged — outside the declared write surface; the spine commit is CEO-owned.
- Task branch has no merge-base with origin/main (same as v0.5), so the pre-handoff rebase cannot be performed safely.
- cold-start 9.536 incumbent is again called "best measured" in LLM final summaries (known metric-vs-summary caveat; the reported metric counts only the 288 tested variants).
- Mechanism sentence: **an escape action that re-ranks candidates by the same statistic (predicted mean) as the strategy it is escaping cannot leave that strategy's basin — it degenerates to the greedy batch whenever the escape filter (here: a shared-mutation signature) is empty.** Search: `git grep -n "signature\|redirect\|diverse" agent/ evolution/ models/` plus reading the acquisition paths (`agent/auto_researcher.py` `list_pool`/`compose_batch`/`redirect_batch`, `evolution/pool_campaign.py` `_rank`). Found: the sibling is `list_pool(by="diverse")` / the v0.4 deterministic alternation — the only acquisition in the repo that carries a variance term (mean+3√var), which is exactly why it, and not any mean-only path, reached 8.4162 (peak UCB-rank #15 vs mean-rank #2930 at the r3 state); no other caller ranks by composition or variance, so no other site silently suffers the degeneration, but any future reuse of `_best_cluster_signature` on a landscape whose measured top-10 lacks a ≥50%-shared substitution will reproduce it. Documents that inherit the falsified premise ("deterministic pure-greedy 8.416"): `agentic-v0.4/report.md`, `agentic-v0.5/report.md`, fact F-F7BC7554, `frontier-landscape-synthesis.md` — re-annotation is CEO-owned.
