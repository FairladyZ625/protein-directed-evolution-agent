已完成并停在本地提交，未 push、未建 PR。

- 提交：`b65eb6fd238287214f48af71942c0b1776f099c3`
- 分支：`codex/task-d72b-pipeline-hardening`
- Worktree：`/private/tmp/ai4s-task-d72b`
- 作者：`ZeyuLi <zeyuli@users.noreply.github.com>`
- 仅修改：
  - `agent/pipeline.py`
  - `tests/test_agent.py`

结论：

- 两个 LLM 角色通过 PydanticAI `Agent(..., output_type=<Pydantic schema>)` 执行。
- 异常及非 schema 返回均进入确定性 fallback。
- MutationDesigner 与 `pool` 可还原的实测变体集合求交。
- 新增阳性对照，明确断言集合外 `IEGV` 被拒。
- 保持 `run_pipeline` 签名不变。
- 已 rebase 最新 `origin/main`。

工具证据：

```text
10 passed, 1 warning in 0.46s
```

`git diff --check` 通过，worktree 干净。已记录 Harness Fact：`F-897849E5`。

风险：

- 当前 `evolution/campaign.py` 传入的是训练子集而非完整 149,361 白名单，因此求交后无法提名训练集外候选。该文件不在本任务授权面内；调用方下一步需传入完整可测空间或等价白名单。
- 唯一测试警告来自 PydanticAI 依赖对 Python 3.13 event loop 的弃用提示，不影响测试结果。