# Independent execution review

- Task: `task_507fa9a9996a458a5b0abfe0d5`
- Execution: `exe_e1eee94f119d4b4a1dcd748366`
- Iteration: 0
- Frozen submission digest: `sha256:8baac074ef1b759c8a57bd0a5dc2659acd6f3e2c964b2b0b32e5d586ec305583`
- Frozen delivery commit: `a3a979adcc478eca7f8fe1f444fa9c5fc121fd5b`
- Verdict: **changes_requested（打回）**

## Basis and observed evidence

1. `ha task show` and the managed execution record both reported `in_review` / `submitted`, iteration 0, commit `a3a979adcc478eca7f8fe1f444fa9c5fc121fd5b`, with the same completion claim and three frozen deliverables supplied by dispatch. No replacement cut was observed before review.
2. The task contract requires AAV ESM-2 and avGFP pool-campaign metrics, event streams, curves, plus `artifacts/harder-findings.md`. Direct `git cat-file -e <commit>:<path>` checks found all of the following absent from the frozen commit: `reports/pool_metrics_aav_esm2.json`, `reports/pool_events_aav_esm2.jsonl`, `reports/figures/pool_curve_aav_esm2.png`, both checked avGFP metric variants, and the task-specific `artifacts/harder-findings.md`. The submission itself explicitly records AAV-ESM full scoring, avGFP data/run, and cross-dataset comparison as deferred gaps.
3. A clean archive of the frozen commit ran `python -m pytest tests/test_pool_campaign.py -q`: `3 passed in 1.22s`. Those three tests cover synthetic bootstrap variance/reproducibility, explicit avGFP WT parsing, and full-sequence ESM backend input.
4. Bootstrap control check on the frozen code: five members produced variance min/mean/max `0.0003810905 / 0.0030525604 / 0.0131031318`; the one-member control produced `0 / 0 / 0`. Thus the implemented bootstrap uncertainty is non-trivial on the synthetic fixture and the check is capable of detecting the degenerate case.
5. Source inspection at the frozen commit found `analysis/agentic_postmortem.py` opts into `RidgePredictor(standardize=True, alpha=10)` for ESM, but the contracted campaign path `evolution/pool_campaign.py::_predictor()` always returns `RidgePredictor(seeds=5)`. Therefore the ESM standardization introduced by the delivered commit is not used by `python -m evolution.pool_campaign --dataset aav --feature esm2`.
6. The submitted `reports/agentic_postmortem_aav_esm.json` is valid JSON and reports a predictor/uncertainty postmortem, not the required four-strategy, three-round pool-campaign result. It cannot substitute for the contract outputs.

## Defects requiring correction

1. **Contract goal and primary outputs are incomplete.** No AAV-ESM four-strategy campaign artifacts, no avGFP campaign artifacts, and no cross-dataset findings were delivered. Deferring nearly the entire experimental goal to another task is a scope change not authorized by this task contract.
2. **The main ESM campaign path does not use the delivered ESM Ridge configuration.** Feature identity/configuration must reach predictor construction so the actual AAV/avGFP ESM campaigns use the justified preprocessing and regularization, with a regression test through the campaign path.
3. **Verification does not meet the declared evidence protocol.** The target test file contains three tests, not coverage for all items named by the contract: four strategies sharing the same fixed pool/budget, random as the weaker control on a deterministic fixture, multi-round reproducibility, and auditable non-trivial variance in campaign events. The closeout's `42→43` statement is not tied to an observed command/output and is inconsistent with the submitted target file's three-test run.
4. **No evidence supports the required semantic conclusion.** The frozen artifact has no honest per-dataset comparison of random/greedy/agent/knowledge, no agent-vs-greedy finding, and no one-hot-vs-ESM pool-campaign comparison. The postmortem's variance correlations answer a different question.

## Actionable repair direction

- Wire dataset/feature-aware predictor configuration into `run_pool_campaign` (prefer explicit dependency/config injection over hidden global behavior), then test that ESM campaigns select the standardized configuration while one-hot remains unchanged.
- Run the exact AAV ESM-2 campaign and commit its metrics JSON, verified event JSONL, and curve. Report variance min/mean/max from campaign events and all four strategy outcomes.
- Obtain the real ProteinGym avGFP dataset and establish WT from authoritative metadata or an explicit validated WT row; run at least one contracted feature configuration and commit the corresponding metrics/events/curve. If external access is genuinely blocked, follow the task checkpoint and return a blocker rather than submit the task as complete.
- Produce the task-specific `artifacts/harder-findings.md` with numerical GB1/AAV/avGFP comparisons and an explicit, honest agent/knowledge-vs-greedy conclusion, including negative results.
- Expand `tests/test_pool_campaign.py` to exercise the declared pool semantics, random control, repeatability, and emitted non-zero variance through `run_pool_campaign`; rerun the named test and attach exact output.

## Unverified

- The claimed 2.6 GB local model cache and 173 MB/38,265-row embedding cache are local, non-delivered state and were not accepted as frozen artifact evidence.
- No full AAV/avGFP campaign was rerun because the required frozen inputs and outputs were not part of this submission.
