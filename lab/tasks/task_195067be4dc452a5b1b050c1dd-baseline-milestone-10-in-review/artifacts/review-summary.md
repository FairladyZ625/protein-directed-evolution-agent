- `task_84fd80d36f4b5a871d7478a8ff` — **changes_requested**：提交快照含三级 `fit/predict(mean,var)`、ESM-2/one-hot 指标 JSON（Spearman/Pearson/MSE/Top-k），本轮合成复验得到 Spearman 0.991386、shuffle 0.114365、非零方差且可复现；但契约点名的 `tests/test_predictor.py`、模型对比图和 Top-k 高-fitness 识别分析缺失。修复：补持久化指标/复现/shuffle 测试、对比图及 Top-k 分析。生命周期：`review-execution` 未写入，Harness 报 `executor_binding_invalid`。
- `task_823620225b20cdb87c7ddbd1bd` — **changes_requested**：`tests/test_events.py` 的篡改阳性对照、fsync、投影/replay 和 200 线程单实例并发均通过；但 closeout 所称“文件锁串行化”不成立，代码只有每实例 `threading.Lock`，本轮两个 `EventStore` 实例并发产生 seq `[1,1]` 且 `verify()` 报断链。修复：按单写者边界更正 closeout，或实现/测试真正跨实例文件锁。生命周期：review 未写入（同一 reviewer 绑定门）。
- `task_cea947b5025412586587d1e4b1` — **changes_requested**：规则/图谱/消融测试通过，20 aa 属性和 6 个稳定 ID 实存；但 BLOSUM62 表仅是部分条目，`C39M` 完全绕过 BLOSUM 判定，`R-PRIORITIZE-HISTORICAL` 只声明不执行，故“6 条约束覆盖”不成立。修复：补全 20×20 BLOSUM62（或权威库）并实现历史优先规则与反例测试。生命周期：review 未写入（同一 reviewer 绑定门）。
- `task_d72b24d5452f2e96e6a94769b1` — **changes_requested**：五角色、事件、规则拒收和 injectable callable 测试通过；但主链未使用 PydanticAI `Agent(..., output_type=...)`，`agent/llm.py` 直接返回 raw OpenAI 文本，且 `MutationDesigner` 本轮实证会生成真值表外 `AAAC`。修复：在 Hypothesis/Critic 两处接商业 API 的 PydanticAI 结构化输出，并把 149,361 measured-set 过滤置于 Agent 边界、加越界反例。生命周期：review 未写入（同一 reviewer 绑定门）。
- `task_afc2590161f244b08a0ce9216e` — **approved（内容）**：GB1 hard 指标实证 candidate space=149,361、budget=96、四策略同 oracle，AAV 池式指标同预算；两条事件流分别 50/44 事件且哈希链通过。旧 AAV“均卡 7.53”只代表该版本，已被 `F-885537A3` 取代（greedy 赢 strong=166；UCB β=3 赢单峰 30/30；LLM 自主均未匹配）。生命周期：review/consent 未写入，未到 CI 门，阻于 reviewer session 只能由原 executor 派生。
- `task_17056dd3fd8bb06bd69b91d499` — **approved（内容）**：`app/demo.py` 单文件三模块实存，AppTest 覆盖四策略、五角色回放、哈希链、真值表外提示、一轮内存推荐与只读红线；本轮相关定向套件合计 32 passed。生命周期：review/consent 未写入，未到预期 CI 门，阻于 reviewer 绑定授权。
- `task_507fa9a9996a458a5b0abfe0d5` — **approved（按本轮基础设施缩限）**：bootstrap Ridge 方差、avGFP 显式 WT loader、全长 ESM 接口三项测试通过；本机 ESM-2 650M checkpoint 2.4 GiB、AAV 38,265 条 embedding cache 173 MiB 实存，avGFP 原始 CSV/完整跑分明确延期。生命周期：review/consent 未写入，未到预期 CI 门，阻于 reviewer 绑定授权。
- `task_ed9e41ef27c8fb94e8a99062a8` — **approved（内容）**：`harder-dataset-survey.md` 实存并含 6 个候选×6 项判据、Top-1 AAV、avGFP 备选、接入方案和 `[需核实]` 风险清单，`F-51F2E349` 及 produces 关系成立；最终策略定性仍以 `F-885537A3` 为准。生命周期：直接 review 与 reviewer dispatch 均报 `executor_binding_invalid`，因此未 consent/complete（docs-task 的无 CI 路径也未能进入）。
