verdict：打回（changes_requested）

已通过 Harness 记录独立评审 `review_de_reviewer_20260914_kb_iter2`。

主要证据：

- 当前 v07 文档存在，交付却称不存在。
- 当前无标签复核结果：`gate=2515`、`gate_residue_pairs=3878`、`unseen_residue_pairs=2323`、相关候选 `1770`；文档仍使用旧的 `9533/11130/7937/6852`。
- BLOSUM 当前缺失无序对为 0，显式零分均保留为 0。
- `build_knowledge_graph` 已在 `agent/auto_researcher.py:488-508` 接入，文档现状表述仍冲突。

修复方向：按当前代码和 read-set 重跑并统一正文、附录、结论；旧结果只能明确标为历史，不能继续作为当前依据。保留收益尚未证明、answer-agnostic 边界及 ESM 负例的谨慎表述。