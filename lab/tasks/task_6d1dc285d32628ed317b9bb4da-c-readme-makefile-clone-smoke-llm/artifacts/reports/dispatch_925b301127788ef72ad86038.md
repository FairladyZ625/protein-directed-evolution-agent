Checkpoint reached: `agent/llm.py` resolved the actual model ID as:

```text
resolved_model=claude-sonnet-5
```

No files changed yet, per the task contract’s mandatory stop before correcting labels. Please confirm to proceed with the scoped README/Makefile/smoke and attribution updates.