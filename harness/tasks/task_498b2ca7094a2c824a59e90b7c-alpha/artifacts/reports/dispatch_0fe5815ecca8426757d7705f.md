任务完成，所有交付已落盘并本地提交。最终报告：

---

## 结论

五个「有数据没界面」的分析面板已全部接入 `app/demo.py`,以第④⑤两个新 tab 并列呈现，模块①②③ 行为未动。门控 **`pytest tests/test_demo.py tests/test_demo_app.py` 19 项全绿**(101s),已停在本地 commit **`dd02300`**(作者 ZeyuLi,feat 前缀，不 push)。

**新增面板与口径**(全部数字取自源 JSON 同名字段，界面不重算；图表文字英文)：

| Tab | 面板 | 数据源 | 关键口径标注 |
|---|---|---|---|
| ④ | 位点集中分析 | `workflow-v1.1/gb1/campaign_easy.metrics.json` 的 `topk_concentration` | 按策略×位点柱图残基分布 + dominant/mutation_fraction |
| ④ | 组合理由 | `workflow-v1.1/agent_combination_rationales.json` | `evidence_source` 与 `narrative_source` 分列；明示「确定性叙事，不是 LLM 推理」 |
| ⑤ | 突变阶数 | `analysis-v0.1/{gb1,aav}/mutation_order.json` | AAV 0..2→3 行**留出 0.9094 vs 测试 0.6155**(同阶留出系统性高估)；覆盖塌方 **1.000→0.312→0.022** |
| ⑤ | 保守位点 | `analysis-v0.1/{gb1,aav}/conservation.json` | 「自然度先验，**未接入采集或筛选路径**」置顶；真峰名次 **D0Q 第1 / V18A 第4 / S17E 第26** 按负结果呈现 |
| ⑤ | alpha 扫描 | `workflow-v1.0/gb1/predictor_alpha_sweep.json` + `scaling_ablation.json` | 固定 alpha **0.4911→0.2944** 随预处理反向，调参后收敛 **0.4893/0.4916** |

## 运行证据

- **门控**:`19 passed in 101.18s`(含改名阳性对照测试)。
- **阳性对照**:`test_conservation_panel_degrades_when_artifact_missing` 临时改名 AAV `conservation.json` → 面板给出「缺哪个文件 + `features/conservation.py` 哪条命令」提示、无异常、口径标注仍在且不再显示 D0Q 名次 → finally 恢复(已核实文件在位)。存在性检查刻意放在 `@st.cache_data` 之外，缓存不会掩盖缺失。
- **AppTest 渲染证据**：七个 tab 标签齐全；各面板关键串实测输出，如 `dominant_residue L · dominant_fraction 0.2`、`留出验证 0.9094 vs 测试 0.6155`、`complete_fraction 2 阶 1.000 → 3 阶 0.312 → 4 阶 0.022`、`V18A（pos 19，WT V）→ conservation_rank 4 / 28` 等，均可与源 JSON 逐字段对回。
- **streamlit 真实启动**：headless 模式 HTTP 200、health ok。
- **只读红线**：demo 源码无 `write_text/to_json/to_csv/.open(/EventStore(`(测试门内含此断言);git 只改 `app/demo.py`、`tests/test_demo_app.py`、`README.md` 三个声明文件。

## 任务计划前提的一处修正(fact F-281F1B9F)

计划所述「3 个既存失败」在当前 HEAD(7aea424)实测**只剩 1 个**:`test_module1/test_module2` 单独跑与全量跑均通过(模块①②在 AppTest 下正常渲染)。唯一失败 `test_recommender_button_runs_one_round_in_memory` 是 **8474b6e 重写模块③(b)文案后测试断言未跟**(断言的 `llm_source=`/「本轮五角色推理链」已被删除)，不是渲染缺陷。我按现行 UI 契约更新了断言(保留「模型预测非实测」「内存 recorder」诚实标注的语义检查)，未削断言。

## 风险与说明

1. **实现过程中抓到并修正了一个自引入错误**：真峰名称映射最初按 1-based 位点键控，导致 V18A/S17E 名次互换；已改按 JSON 的 `position_zero_based` 映射，并用三元对应断言(`名称↔位点↔名次`)钉死防回归。
2. alpha 面板需同时读 sweep 与 scaling_ablation 两份文件才完整呈现「为什么不能固定 alpha」(计划引的 0.4911/0.2944 出自后者，非 sweep 文件)；两文件任一缺失时分别降级提示。
3. 模块②/③共用的 mutation_designer 表格因 candidates 新增 `combination_rationale`(int 键 dict)会触发 streamlit/pyarrow 自动修复警告——非致命、渲染正常，属模块②既有行为，未动(修复它会改模块②渲染面，超出本任务边界)。
4. GB1 的 `true_peak_positions` 为空，保守性面板如实显示「未定义单峰位点」，不伪造。

## 下一步(CEO 验证清单)

`streamlit run app/demo.py` 逐面板点检；抽查面板数字与源 JSON 一致；临时改名任一 JSON 确认降级提示。fact F-281F1B9F 已挂到任务，可直接进入 closeout 流程。