已完成并本地提交：`8474b6e feat(demo): recommend mutations from entered wild type`

- Demo 现接受 4 位点 WT 输入，按输入生成 76 个单点候选、重算相对突变和预测增益。
- 推荐表明确标为模型预测；WT 无实测基准时显示“模型预测值而非实测值”。
- 保持只读；标准 `VDGV` 的 campaign 回放仅使用内存 recorder。
- 新增 4 项逻辑测试与 README 说明。

验证：

- `.venv/bin/pytest tests/test_demo.py` → `4 passed`
- 阳性对照：故意固定候选菜单到 `VDGV` 时，“不同 WT 推荐集合不同”的断言确实失败。
- 已记录事实：`F-D6687614`
- 工作树干净，仅包含 `app/demo.py`、`tests/test_demo.py`、`README.md` 三处提交改动。

风险/下一步：自定义 WT 当前限定为 GB1 四位点口径；现有 `run_campaign` API 固定标准 WT，因此自定义输入展示的是 Ridge 预测，不会误展示标准 GB1 oracle 结果。未实际启动 Streamlit 浏览器界面。