# Facts

Managed by `ha fact record`; hand edits are rejected.

## Records

### F-26276DB5

- Statement: harness/ 是嵌套 git 仓,由 daemon 的 SQLite Outbox 自动提交,「不存在 daemon 自动 git 提交通道」这一我先前的判断为假。实测:harness/.git 存在,提交者 Harness SQLite Outbox <harness-sqlite-outbox@local.invalid> 共 1878 个提交、2026-09-14 当日 422 个,信息形如 harness sqlite outbox outbox-<revision>,其 revision 与 ha settings read 的 entityRevision 对应(最新 b7c84a6 / 13:42:06 / outbox-2936,对应把 reviewReturnBudget 改为 10 的那次写入);经 ha doc sync --submit 的文档在嵌套仓内状态为已提交/无差异。回执里的 git: pending 实指主仓一侧未提交——主仓另外跟踪 harness/ 下 1582 个文件,故那一侧长期显示 M,与嵌套仓的 outbox 提交是两件事。我的错因:只在主仓 git status 里看,且只查 ha daemon --help 的子命令列表,从未查过 harness/.git。后果:据此向 CEO 上报了一个不存在的阻塞(task_feb731c7 要求授权手动提交 harness/ 文件),该上报已作废。
- Evidence source: harness/.git; git -C harness log --format='%an <%ae>'; git -C harness status --porcelain; ha settings read(entityRevision=2936)
- Observed at: 2026-09-14T13:47:22.985Z
- Confidence: high
- State: standing

