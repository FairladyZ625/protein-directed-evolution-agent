# Independent execution review

## Verdict

changes_requested（打回）

评审固定对象：task `task_d72b24d5452f2e96e6a94769b1`，execution `exe_2d7e739db6e6afc3d2ccb7976b` iteration 0，submission digest `sha256:fb9eb9d4e9888bfde9efbc513eb22bd81344e8dc2d98fb9c5a2a4916f74eed9f`，commit snapshot `4f456890ca43dfe007e4b17c63c06c3fa26f147f`。

## Tool-grounded evidence

- 从 `git archive 4f456890...` 创建隔离快照，并在该快照运行 `.venv/bin/python -m pytest -q tests/test_agent.py`：`6 passed in 0.20s`。
- 实读冻结 `agent/pipeline.py`：`HypothesisGenerator` 与 `ScientificCritic` 仅接收普通 `Callable`；文件中没有 `pydantic_ai.Agent` 或 `output_type`。`agent/llm.py` 只有直接调用 `openai.OpenAI` 的 `chat_json`，未提供并接入两个 Pydantic schema adapter。因此提交所称“五角色 PydanticAI 流水线”不成立。
- 对 LLM/schema 失败做阳性对照：注入会抛出 `ValueError("malformed/schema failure")` 的 hypothesis port，`run_pipeline` 输出 `LLM_SCHEMA_FAILURE_PROPAGATES ValueError malformed/schema failure`。实现没有捕获、校验或确定性 fallback，命中 task checkpoint“LLM schema 遵循失败无降级”。
- 对 D1 实测空间门做阳性对照：pool 仅包含 `V39I` 和 `D40E` 两条实测记录，注入引用规则 ID 的双突变假设后，流水线提名 `VEGV`、`IDGV`、`IEGV`；其中组合 `IEGV` 不在传入实测集合，输出明确为 `OUTSIDE_SUPPLIED_MEASURED_SPACE ['IEGV']`。实现仅限制合法位点/WT 锚点，未接收或查询 149,361 真值集合。
- 实读 `tests/test_agent.py`：所谓空间测试只检查 `A10G` 被 off-site guard 丢弃，没有构造“合法四位点但不在 149,361 实测表”的候选；因此 `6 passed` 不能证明 D1。
- 冻结 deliverable 是整个 `agent/`；其中 `agent/auto_researcher.py` 另有 `Agent(oa, system_prompt=...)`，既未设置 `output_type`，LLM actor 也不是 Hypothesis Generator 或 Scientific Critic。若该文件被视为本任务交付的一部分，还直接违反“LLM 只在两处”的目录级声明。

## Defects

1. **阻断：未实现契约指定的 PydanticAI schema 节点。** 两个 LLM 节点只是可注入 Python callable；生产 LLM helper 是裸 OpenAI chat completion，未以 `output_type=Hypothesis/CriticSchema` 约束输出。
2. **阻断：LLM/schema 失败没有降级链。** port 的异常会终止整轮，和 task checkpoint 直接冲突。
3. **阻断：没有 149,361 实测空间 membership gate。** 四个位点合法不等于存在真值；当前设计能组合出输入实测集合外候选，存在下游查表失败或错误补零风险。
4. **测试存在语义假绿。** 当前测试覆盖 off-site、BLOSUM 拒绝、budget 和事件 actor，但没有覆盖 PydanticAI output schema、异常/无效结构降级、合法位点缺真值组合拒绝。
5. **交付边界混入额外 LLM。** `agent/auto_researcher.py` 与本任务“五角色仅两处 LLM”口径冲突；需要缩窄 deliverable 或明确隔离并避免用该目录整体支撑受控两处声明。

## Required fixes

- 为 Hypothesis Generator 和 Scientific Critic 分别建立真实 `pydantic_ai.Agent` adapter，显式使用 Pydantic `output_type`；底层 provider 保持商业 API。pipeline 只调用这两个 adapter，其他三角色保持确定性。
- 在两个 adapter 周围实现可审计降级：API 异常、超时、JSON/schema validation failure 均落事件并进入确定性 fallback；新增 fake model/port 测试证明无效输出和异常都能继续完成流水线。
- 给 `MutationDesigner` 或编排入口注入 canonical measured-sequence set（149,361 条），在评分前做 membership filter；事件记录拒绝数量/原因。测试至少构造一个四位点与氨基酸均合法、但不在真值集合的组合，并断言不进入 predictor。
- 将 `agent/auto_researcher.py` 从本任务冻结 deliverable 中拆出，或把它的独立任务边界写清并修正“整个 agent/ 仅两处 LLM”的交付声明。

## Residual / unverified

- 未调用真实商业 API；本次阻断点可由冻结代码与离线阳性对照确定，无需消耗外部 API。
- 未运行全量测试矩阵；按任务契约只运行 `tests/test_agent.py`，并补充两个定向阳性对照。
- 事件流 actor 测试已通过，但未因上述阻断项进一步生成并核验真实 `EventStore` 回放样例。
