# Architecture Context

## Purpose

Record durable system boundaries, component responsibilities, relationships, and the reading routes that support architectural decisions.

## Opt-in Boundary

Initialization creates only this guide. It does not create or enable an architecture manifest, model, or generated view.
