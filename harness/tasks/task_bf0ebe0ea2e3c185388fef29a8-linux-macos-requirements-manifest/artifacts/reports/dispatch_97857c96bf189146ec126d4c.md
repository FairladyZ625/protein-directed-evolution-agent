已完成并本地提交：`c763e7f feat(delivery): add reproducible install and run scripts`

- 新增 Linux/macOS 兼容的 `scripts/install.sh`、`scripts/run_all.sh`，默认轻量安装，`--full` 安装 torch/ESM。
- 补充 Makefile、README、依赖分档、7 个缺失 manifest。
- 台账追加 9 条可追溯记录；链校验通过，共 24 条。
- 验证：`make smoke`、缺数据自动降级、`pytest -q tests/test_predictor.py tests/test_campaign.py` → 15 passed。
- 交付验证记录已发布到任务 artifacts。

风险：干净 `/tmp` 安装因运行器 30 秒下载限制未完成；Linux、`--full` 与有数据的完整分支未验证。Rebase `origin/main` 被已有未跟踪的 `data/aav/aav`、`data/pools/pools` 安全阻止，未改动它们。