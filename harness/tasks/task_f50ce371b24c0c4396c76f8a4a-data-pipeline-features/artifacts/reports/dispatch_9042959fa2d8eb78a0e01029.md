# Independent execution review

## Verdict

changes_requested（打回）

## Tool-grounded evidence

- Frozen submission digest: `sha256:6f35e98d0b222339b88d30d944e76d9f2f971ac59111eaa3456c86296da7a980`; frozen delivery commit: `54821e70643b856b78176c13889208d67bb559b9`.
- `git cat-file`/`git ls-tree` confirmed the frozen commit contains the expected implementation snapshot, including the three pool CSVs, `evolution/mutations.py`, `features/`, `tests/test_data_pipeline.py`, the full one-hot cache, and a keyed 5,000-row train ESM cache.
- Every locally inspected core file was first verified byte-identical to its blob in the frozen commit with `git hash-object` versus `git rev-parse <commit>:<path>`; no unpinned local implementation was substituted.
- `.venv/bin/pytest -q tests/test_data_pipeline.py` returned `6 passed in 0.94s`.
- Frozen one-hot cache: variants `(149361,)`; embeddings `(149361, 80)` `uint8`.
- Frozen train ESM cache key is `dbfd1ecc45bd4b72148d`; variants equal the ordered 5,000-row train pool; embeddings are `(5000, 1280)`, finite, and non-constant. Overlapping variants in two other frozen ESM archives match it exactly (`max_abs=0.0`).
- Cache detector positive control: two intact reads invoked the injected backend once total; after corrupting cached variant order, the next read invoked it again (`1 -> 2`), proving mismatch detection is active.
- The frozen deliverables supplied for this dispatch contain no declared T2 core delivery paths (`data/`, `features/`, `evolution/mutations.py`, `tests/test_data_pipeline.py`). Independent commit-delta inspection likewise found 1,054 changed paths, zero in the contract surface, including deletion of `HARNESS-UX-FEEDBACK.md` and committed `pytest-of-root` artifacts.

## Defects

1. **Blocking — delivery is not task-scoped.** The task contract permits only `data/`, `evolution/mutations.py`, `features/`, and this task's tests, while the frozen deliverables are a broad repository/governance/report import plus a production-path deletion and pytest temporary files.
2. **Blocking — the submitted delivery set does not carry the claimed T2 implementation.** The implementation exists in the frozen commit snapshot and passes tests, but none of its required core paths is in the frozen deliverables/delta. Therefore this execution cannot prove that its delivery produced the contracted code and caches.
3. **Evidence limitation — real ESM extraction equivalence remains unverified.** The directed cache test uses an injected deterministic backend. The committed cache is internally consistent and usable, and the closeout discloses the machine-local weight limitation, but this run did not independently compare it with a live 650M forward pass.

## Actionable repair

- Amend/resubmit the execution with a task-owned delivery cut containing only the contracted T2 paths and required cache files; exclude all harness/report/app/research history, `pytest-of-root`, and unrelated deletions.
- Ensure the frozen deliverables explicitly include the three pools, mutation/data validation code, feature encoders, full one-hot cache, keyed 5,000-row train ESM cache, and `tests/test_data_pipeline.py`.
- Preserve the current honest ESM residual-risk wording. If claiming actual live-versus-cache equality, attach a real 650M forward-pass comparison; otherwise describe the test accurately as cache semantics tested with an injected backend.

