# Facts

Managed by `ha fact record`; hand edits are rejected.

## Records

### F-35BD26B9

- Statement: CI 在 main 上连续多次 push 全红而无人发现:.github/workflows/ci.yml 手写的 pip 清单与仓库 requirements.txt 漂移了,清单里没有 pydantic-ai,而 agent/pipeline.py 顶部就是 from pydantic_ai import Agent,于是 tests/test_agent.py、test_campaign.py、test_demo.py 三个文件在 pytest collection 阶段即 ImportError,pytest 以 exit code 2 中断——被门控的六个文件里有三个一个测试都没跑。gh run list 显示至少最近 6 次 push 全是 failure。这是 F-6B21C4E8(没被门控的测试等于不存在)的同族第二次:上一次是测试文件没被列进 CI,这一次是列进去了但依赖没装,症状都是「门看起来在、实际没关」。根治方向是让 CI 从仓库自己声明的依赖文件安装而不是维护第二份清单;当前已补齐缺失依赖并在 ci.yml 写明待轻量/完整依赖分档合入后改为 pip install -r requirements.txt。教训:本地全绿不能替代 CI 绿,我此前报告「测试全绿」用的是本地证据而 CI 同期在红。
- Evidence source: .github/workflows/ci.yml:26-40; gh run view 34702871905 --log-failed; gh run list --workflow ci.yml
- Observed at: 2026-09-12T15:47:01.669Z
- Confidence: high
- State: standing

