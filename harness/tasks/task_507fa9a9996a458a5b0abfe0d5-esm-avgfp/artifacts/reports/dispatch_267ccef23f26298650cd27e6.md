Verdict：打回（`changes_requested`）

已通过 Harness 记录独立评审：

- Execution：`exe_e1c708231c47d10d40ad509581`
- 冻结提交：`fe5a1a5aa7140a8266a6d99b007a298800856c50`
- Review：`review-dispatch_267ccef23f26298650cd27e6`
- Harness 结果：`accepted_durable; projection: verified`

工具证据：

- 冻结归档运行 `python -m pytest tests/test_pool_campaign.py -q`：`3 passed in 1.35s`。
- Bootstrap 阳性对照：5 成员方差 min/mean/max 为 `0.0003810905 / 0.0030525604 / 0.0131031318`。
- 阴性对照：1 成员方差为 `0 / 0 / 0`。因此不确定性机制本身有效。
- 冻结提交中缺少 AAV-ESM metrics、事件流、曲线及全部 avGFP campaign 产物。
- [harder-findings.md](/Users/lizeyu/Projects/ai4s-directed-evolution-agent/artifacts/harder-findings.md) 明确承认 AAV ESM 下载中断、avGFP 未运行，尚无跨数据集 agent-vs-greedy 结论。
- `pool_campaign._predictor()` 仍固定构造 `RidgePredictor(seeds=5)`，未解决此前指出的 ESM 特征专属标准化/配置问题。
- 现有三个测试未覆盖任务要求的四策略同池、random 阴性基线、多轮复现及 campaign 事件方差。

具体缺陷：

1. AAV-ESM 和 avGFP 四策略实验均未交付，核心 Goal 未完成。
2. 缺少相应 metrics、事件流和曲线。
3. 跨数据集报告没有真实数值，无法回答 agent/knowledge 是否优于 greedy。
4. Findings 文件路径也不符合契约指定的任务 `artifacts/` 位置。
5. Closeout 将核心工作延期，属于未经授权的任务缩减。
6. “已合并 master”不能替代当前 execution 的冻结交付证明。

修复方向：

- 完成 AAV ESM-2 campaign，并提交 metrics、可验证事件流和曲线。
- 获取真实 ProteinGym avGFP 数据，权威确认 WT，完成至少一种特征下的相同实验。
- 让 predictor 配置显式感知特征类型，并增加主 campaign 路径回归测试。
- 补齐四策略同池、random control、多轮复现、事件方差测试。
- 在任务目录的 `artifacts/harder-findings.md` 写入逐数据集真实数字和诚实结论。
- 若下载仍受阻，应按任务 checkpoint 报 blocker，不应再次按完成态提交。