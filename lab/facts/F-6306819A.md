# Facts

Managed by `ha fact record`; hand edits are rejected.

## Records

### F-6306819A

- Statement: 真实 campaign 的两条 agent 策略跑 0 轮且静默、退出码 0;根因是把『已测过什么』与『oracle 能测什么』混为一谈,而该缺陷被一个测试夹具遮盖、CEO 验收时漏过。机制:包2 给 agent/pipeline.py 的 run_pipeline 加了『候选须与实测集合求交』(修复『提名 oracle 查不到的变体』,方向正确),但它从入参 pool 推导可测集合;evolution/campaign.py 传的是 _pool_records(train) 即**已测过的训练集**。提名本来就还没被测过,于是求交把每一个新提名都滤掉。实测后果:重跑 lab/reports/workflow-v1.1/gb1/campaign_easy.metrics.json 里 strategies 四键齐全但 agent_no_knowledge 与 knowledge_agent 的 rounds 长度为 0、cum_top10_max 为 None,summary 只有 random 与 greedy 两条,进程退出码仍为 0、无任何报错。验收失职点:集成 worker 当时用 autouse monkeypatch 给测试注入完整实测空间,其 docstring 明写『裸 _pool_records 只含冷启动历史,必须补充,否则求交会正确地滤掉每个新提名』——那句话描述的是生产缺陷,CEO 读成了测试夹具说明,只核了『测试改动纯新增、未削断言』(该判断本身成立)就放行。修复:run_pipeline 增加显式 measurable_variants 参数(None 时回退到 pool 推导,仅当 pool 已覆盖整个空间时才正确);campaign 传 measurable_variants=set(df.Variants);删除那个 monkeypatch 夹具,使 tests/test_campaign.py 里既有的 n_nominated == [2, 2] 断言成为真门。删除夹具后 tests/test_campaign.py 与 tests/test_agent.py 共 20 passed,证明生产路径已通。教训:测试夹具里对生产行为的解释性 docstring 要当缺陷报告读,而不是当夹具说明读。
- Evidence source: lab/reports/workflow-v1.1/gb1/campaign_easy.metrics.json(strategies 四键、agent 两键 rounds 为空); agent/pipeline.py:run_pipeline; evolution/campaign.py:_agent_propose 的调用点; tests/test_campaign.py 被删除的 _supply_complete_measured_space 夹具
- Observed at: 2026-09-12T14:33:38.231Z
- Confidence: high
- State: standing

