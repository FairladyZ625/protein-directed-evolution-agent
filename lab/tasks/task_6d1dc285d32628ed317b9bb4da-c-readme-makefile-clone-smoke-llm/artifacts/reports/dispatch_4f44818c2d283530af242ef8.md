已完成并本地提交：`b8b0b60 feat: add self-contained smoke workflow`

- 新增 `make smoke`：固定 16 变体合成地形，离线完成 predictor、五角色 Agent、四策略 campaign，产物在 `tmp/smoke/`，含指标、图、事件链和 SQLite 投影，并明确标注“合成小规模、不可与正式结果比较”。
- `make campaign` 在缺少全量 CSV 时明确提示缺失文件、`data/README.md` 来源说明及 `make smoke` 替代路径。
- README 将 `make smoke` 设为首个推荐命令。
- `make test` 与 CI 自包含测试集合一致。

验证证据：

- `make smoke` 成功，生成 116 个投影事件且事件链已验证。
- `make test`：42 passed，1 warning。
- 从隔离的本地 clone（无 `data/four_mutations_full_data.csv`、无 `features/cache`）执行 `make smoke` 成功。
- 已 rebase 至最新 `origin/main` 后复跑验证。
- 已记录事实：`F-B392F953`。

残余风险：smoke 依赖正常安装 `requirements.txt` 中的 Python 包；不依赖真实 GB1 数据、ESM 缓存或 API key。